# BINISHOP — ENVIRONNEMENT LOCAL
# --- PostgreSQL ---
POSTGRES_USER=binishop
POSTGRES_PASSWORD=binishop_local_dev_2026
POSTGRES_DB=binishop
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}

# --- Redis ---
REDIS_PASSWORD=binishop_redis_dev_2026
REDIS_URL=redis://default:${REDIS_PASSWORD}@localhost:6379

# --- MinIO S3 ---
MINIO_ENDPOINT=http://localhost:9000
MINIO_CONSOLE_ENDPOINT=http://localhost:9001
MINIO_ACCESS_KEY=binishop_admin
MINIO_SECRET_KEY=binishop_minio_secret_key_2026
MINIO_BUCKET=binishop-media
MINIO_REGION=eu-west-1
MINIO_USE_SSL=false

# --- Medusa (port 3005 : 9000/9001 pris par MinIO) ---
MEDUSA_BACKEND_URL=http://localhost:3005
MEDUSA_ADMIN_URL=http://localhost:3005/app
MEDUSA_STORE_URL=http://localhost:3005/store

# --- JWT / Auth ---
JWT_SECRET=binishop_jwt_secret_dev_2026_super_secret
COOKIE_SECRET=binishop_cookie_secret_dev_2026

# --- Flutter ---
FLUTTER_API_BASE_URL=http://localhost:9000
FLUTTER_ADMIN_BASE_URL=http://localhost:9000/admin